Place your Excel file here and name it exactly **Einstein3.0.xlsx**.

The seed script expects this relative path:
server/data/Einstein3.0.xlsx

Then run from the **server** folder:
1) npm install
2) npm run prisma:generate
3) npm run prisma:migrate
4) npm run seed
