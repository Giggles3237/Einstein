# Copy this file to .env and fill in your database connection string.
# MySQL example (adjust host/user/pass/db and SSL options for Azure):
# DATABASE_URL="mysql://USER:PASSWORD@HOST:3306/DATABASE?sslaccept=preferred"
# If Azure requires strict TLS, try: sslaccept=strict
# If you run into cert issues in dev only, you can use (not for prod): sslaccept=accept_invalid_certs

DATABASE_URL="mysql://your_user:your_password@localhost:3306/einstein_db?sslaccept=preferred"
PORT=4000
